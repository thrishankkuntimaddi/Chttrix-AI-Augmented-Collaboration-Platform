{
    "buildCommand": "npm install && npm run build --workspace=client",
    "outputDirectory": "../dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}